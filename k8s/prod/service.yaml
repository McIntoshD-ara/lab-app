# lab-app/k8s/dev/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: lab-app
  namespace: prod
  labels:
    app: lab-app
spec:
  selector:
    app: lab-app
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: NodePort
