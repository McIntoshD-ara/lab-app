# k8s-demo/k8s/dev/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: k8s-demo
  namespace: dev
  labels:
    app: k8s-demo
spec:
  selector:
    app: k8s-demo
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: NodePort